# 3.0.0

Tauri 2 + Vue 3 重写首个 MVP。

## 变更

1. 新架构：Tauri 桌面壳 + Vue3/Vite UI + Node Fastify sidecar（ali-oss）。
2. 支持 AK/STS 登录、Bucket/对象浏览、上传下载队列、基础设置。
3. 会话改用 Tauri Store 安全存储；不安全 TLS 改为设置项显式开启。
4. Electron 2.x 代码迁入 legacy/，继续可通过 Releases 使用 v2.0.0 包。
